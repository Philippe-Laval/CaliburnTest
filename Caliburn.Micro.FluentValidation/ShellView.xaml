<UserControl x:Class="Caliburn.Micro.FluentValidation.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Caliburn.Micro.FluentValidation">
    <StackPanel>
        <TextBox x:Name="Error" Visibility="{Binding Error, Converter={local:StringNullOrEmptyToVisibilityConverter}}" />
        <TextBox x:Name="Name" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />
        <TextBox x:Name="Email" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />
        <TextBox x:Name="Zip" />
        <Button x:Name="SayHello"
                Content="Click Me" />

    </StackPanel>
</UserControl>